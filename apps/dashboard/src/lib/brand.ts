/**
 * Platform identity. Distinct from the practice profile in Settings: the
 * profile is the tenant (Schissel Medicine, PLLC), this is the product the
 * tenant is using.
 */
export const BRAND = {
  name: 'Telemed.ai',
  tagline: 'Global Licensing Platform',
} as const;
